# Release Notes

## Ver 3.1.4 (2026-03-04)

### 主な変更

#### バグ修正・安定性の向上
- **テンプレート引数の引き継ぎ修正**: 共通ナビゲーションをインクルードする際にユーザー情報（`user`）が未定義になる不具合を修正しました。`include` 関数でコンテキストを明示的に引き渡せるように拡張しました。

## Ver 3.1.3 (2026-03-04)

### 主な変更

#### ナビゲーション機能の追加
- **共通ナビゲーションバー**: 画面上部に共通メニュー（ダッシュボード、保護者ページ、切り替え）を追加しました。これにより、管理者と保護者視点の切り替えが容易になりました。
- **UI調整**: 各ページのヘッダーデザインを整理し、ナビゲーションバーと統合しました。

## Ver 3.1.2 (2026-03-04)

### 主な変更

#### パフォーマンスの大幅な改善
- **サーバーサイド・ルーティング**: ログイン後のリダイレクトをサーバー側（GAS）で処理するように変更し、画面遷移の待ち時間を大幅に削減しました。
- **データキャッシュ機能**: ユーザーの役割情報をCacheServiceでキャッシュ（1時間）することで、ページ読み込みごとのスプレッドシートへのアクセスを削減しました。
- **テンプレート埋め込み**: ユーザー情報をHTMLのレンダリング時に埋め込むように変更し、読み込み後の追加通信（google.script.run）を廃止しました。
- **インクルード高速化**: CSSやサブテンプレートの読み込みにグローバルキャッシュを導入しました。

## Ver 3.1.1 (2026-03-04)

### 主な変更

#### ダッシュボード表示の最適化
- **予約連動フィルタリング**: ダッシュボードの児童リストを、当日の「出席記録」があるか、または「予約」がある児童のみを表示するように変更しました。これにより、予約のない児童が表示されて未着アラートが誤作動する問題を解決しました。

## Ver 2.2.4 (2026-03-02)

### 主な変更

#### お迎えアラート機能の再設計
- **全画面アラート表示**: アラート発動時、画面全体が赤色の緊急表示に変わり、最上部に表示。
- **アクションログ機能**: 「今呼びます」「完了」ボタンを押すと、誰が対応したかがアラート上に表示を重ねて記録。
- **取り消し機能**: 「取り消し」ボタンでアラートを即座に解除可能。
- **退室連動解除**: 退室ボタン押下時、該当児童のアクティブなアラートを自動解除。

#### 保護者カレンダーの改善
- **児童別予約表示**: カレンダーの予約ハイライトが、選択中の児童に限定して表示されるように変更。
- **日付フォーマットの統一**: 予約送信時の日付生成を `formatDate` ユーティリティに統一。

#### リスクコードの除去
- `getReturnMethodIcon` の空実装を修正（正しいアイコンを返すように変更）。
- ログインページの未使用変数（`staffId`, `VERSION`）や古いコメントを削除。
- `submitReservations` の日付形式を `toLocaleDateString` から `formatDate` に統一。

---

## Ver 2.2.3 (2026-02-28)

### 主な変更

#### カレンダー機能の修正と強化 (Parent Portal)
- **予約時の児童選択機能を追加**
  - ご兄弟など複数の自動が登録されている場合、予約パネルから対象の児童を選択できるようになりました。
  - 児童未選択時に「日付を選択してください」と表示される不具合を修正。
- **予約内容の編集機能を追加**
  - 確定済みまたは申請中の予約をクリックすると、詳細画面から終了時間やおやつの有無を直接変更できるようになりました。
- **日付判定の正確性向上 (JST対応)**
  - `toISOString()` によるUTC依存の判定を廃止し、日本時間（JST）に基づいた正確な日付判定を行う共通ユーティリティを導入。
  - これにより、午前9時以前に「昨日のデータ」が表示される問題が解消されました。

#### 管理者・職員機能の改善
- **ダッシュボードの表示修正**
  - タイムゾーンの問題で午前中に児童一覧が表示されない問題を修正。
- **職員出勤リストの整合性**
  - 日報メモやステータス管理において、日本時間を基準に「今日」が正しく判定されるように修正。

#### 内部ロジックの改善
- **共通時間ユーティリティの導入**
  - `src/lib/date.ts` を作成し、アプリ全体で一貫した日付形式（YYYY-MM-DD）を使用するように統一。

---


## Ver 2.2.2 (2026-02-24)

### 主な変更

#### 保護者マイページの大幅刷新 (Overhaul)
- **統合カレンダーの導入**
  - カレンダーに「予約」「変更申請」「お知らせ」の3つのモードを統合
  - 予約モード：複数日の一括選択と、サイドパネルからのクイック予約が可能に
  - 変更申請モード：既存予約の編集に加え、過去の日付に対する「修正希望」リクエスト機能を追加
  - お知らせモード：配信されたおたよりをカレンダー上で直接確認・プレビュー可能に

- **ナビゲーションの統合と整理**
  - 旧「予約」ページを廃止し、ホーム画面のカレンダー機能に集約
  - ボトムナビゲーションを整理し、ユーザー体験を向上
  - 未使用のモックナビゲーションを削除

- **児童表示の堅牢性向上**
  - 児童名が表示されない場合のフォールバック処理を強化（フルネーム、ID等を使用）
  - 右上のメッセージボタンを削除し、一貫した導線を確保

- **予約リストの改善**
  - 今後の予約を時系列でソートして表示
  - 初期表示件数を5件に制限し、展開（もっと見る）機能を追加

#### 管理者機能の改善
- **ダッシュボードの利便性向上**
  - 選択した日付を URL パラメータに保存し、リロード後も状態を維持
  - 「今日に戻る」ボタンを追加

- **ログイン動線の固定**
  - 管理者・職員ログイン後の遷移先を管理ダッシュボードに固定

### 破壊的変更
1. **保護者用予約URLの廃止**
   - 旧：`/parent/reserve`
   - 新：`/parent/home` 内の統合カレンダー
   - 既存のリンクは自動的にホームへ誘導されるか、ナビゲーションから削除されています。

---

## Ver 7.1 (2026-02-10)

### 主な変更

#### 用語の統一
- **Guardian → Parent への完全リネーム**
  - すべてのコード、UI、ドキュメントで用語を統一
  - より直感的で理解しやすい命名に変更

#### データベース変更
- **新規コレクション**
  - `parents` コレクション作成（旧 `guardians`）
  - Email ベース認証に統一

- **フィールド名変更**
  - `children.guardianIds` → `children.parentIds`
  - `children.guardianName` → `children.parentName`
  - `authorizedEmails` 廃止（`parentIds` に統合）

#### ルーティング変更
- `/guardian/*` → `/parent/*`
  - `/parent/login` - 保護者ログイン
  - `/parent/home` - 保護者ホーム
  - `/parent/reserve` - 予約管理
  - `/parent/payment` - 支払い管理
  - `/parent/documents` - ドキュメント

#### Security Rules 更新
- `parents` コレクションのアクセス制御追加
- `staff_users` ベースの認証に変更
- 後方互換性確保（`authorizedEmails` サポート継続）

#### GAS スクリプト
- Ver 7.1 デプロイ
- `Master_Guardians` → `Master_Parents` シート名変更
- `GuardianIDs` → `ParentIDs` 列名変更

### 破壊的変更

⚠️ 以下の変更により、既存のコードやデータに影響があります：

1. **ルート変更**
   - 旧: `/guardian/*`
   - 新: `/parent/*`
   - ブックマークや直リンクの更新が必要

2. **コレクション名**
   - 旧: `guardians`
   - 新: `parents`
   - データマイグレーション実施済み

3. **フィールド名**
   - 旧: `guardianIds`, `guardianName`
   - 新: `parentIds`, `parentName`
   - データマイグレーション実施済み

### マイグレーション

データマイグレーションは自動実行済みです。

- `guardians` → `parents` コレクション作成
- `children` レコードのフィールド名更新
- `attendance` メッセージの送信者フィールド更新

### 既知の問題

1. **Turbopack ビルドエラー**
   - `npm run build` が失敗する場合があります
   - 回避策: `firebase deploy --only hosting` を使用
   - 開発サーバー（`npm run dev`）は正常動作

### アップグレード手順

Ver 6.3 から Ver 7.1 へのアップグレード：

1. コードを最新版に更新
2. `firebase deploy --only firestore:rules` で Security Rules デプロイ
3. `firebase deploy --only hosting` でフロントエンドデプロイ
4. スプレッドシートのシート名・列名を手動更新
5. GAS スクリプト Ver 7.1 をデプロイ

---

## Ver 6.3 (2026-01)

### 主な変更
- GAS 統合、データ同期機能
- 管理画面の改善
- CSV インポート/エクスポート機能

---

## Ver 6.1

### 主な変更
- 基本機能の実装
- 認証システム
- 出席管理
- 予約システム
